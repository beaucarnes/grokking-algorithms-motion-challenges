'''
DO IT! CHALLENGE - 8.2 Greedy Algorithm

When passed the array from 'contactapp.py', return the optimal schedule that fits in as many people
as possible.
'''

def schedule(people):
    sorted_people = sorted(people, key=lambda k: k['end_time'])
    scheduled = []
    # Finish this and fill the scheduled array with only the people in the optimal schedule.



    return scheduled

